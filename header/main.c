#include <stdio.h>
#include <stdlib.h>
#include<arbind.h>

int main()
{
    float r;
    printf("Enter the radius of circle:\n");
    scanf("%f",&r);
    printf("The area is:%f",area(r));
    return 0;
}
